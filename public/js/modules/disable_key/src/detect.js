export const isEnter = (key) => {
  return key === 'Enter';
}

export const isLImitMinux = (dataset) => {
  return dataset.limitMinus !== undefined;
}

export const isMinus = (key) => {
  return key === '-';
}
